[build]
builder = "nixpacks"
buildCommand = "npm install && cd packages/shared && npm run build && cd ../backend && npm run db:generate && npm run build"

[deploy]
startCommand = "cd packages/backend && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

