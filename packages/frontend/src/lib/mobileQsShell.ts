/**
 * Vertical space reserved under main content for the fixed QS bottom tab bar
 * (Chat / Hub) on small screens. Must match padding on `app/chat/page.tsx` chat column.
 */
export const MOBILE_QS_TAB_NAV_RESERVE = '4rem';
