[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = [
  "npm ci --include=dev",
  "cd packages/shared && npm run build",
  "cd packages/backend && npm run db:generate"
]

[phases.build]
cmds = [
  "cd packages/backend && npm run build",
  "cd packages/backend && echo '=== Checking dist folder ===' && ls -la dist/ && echo '=== Files in dist ===' && ls -la dist/ || (echo '❌ DIST FOLDER NOT FOUND' && pwd && ls -la && exit 1)"
]

[start]
cmd = "cd packages/backend && npm start"
