[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = [
  "npm ci",
  "cd packages/shared && npm run build",
  "cd packages/backend && npm run db:generate"
]

[phases.build]
cmds = [
  "cd packages/backend && npm run build",
  "cd packages/backend && ls -la dist/",
  "cd packages/backend && test -f dist/index.js && echo '✅ dist/index.js exists' || (echo '❌ dist/index.js NOT FOUND' && ls -la && exit 1)"
]

[start]
cmd = "cd packages/backend && npm start"
